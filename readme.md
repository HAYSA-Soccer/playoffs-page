# HAYSA Playoffs Page

Data lives in `data/teams.json` (auto-generated from a Google Sheet).

- Set `"active": true` to show a team.
- Set `"active": false` to hide a team.
- Record and seed are regular-season and stay static.
- TeamSideline embeds `https://<your-username>.github.io/playoffs-page` via an iframe.
